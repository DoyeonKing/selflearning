# ⚠️ 重要提示

## 📋 词典文件位置

**当前状态**：`med_word.txt` 在 `models/` 目录

**需要操作**：将此文件复制到 `resources/` 目录，因为 Java 训练服务从 classpath 读取：

```
springboot/src/main/resources/med_word.txt  ← 应该放在这里
```

## 🔧 操作步骤

### Windows (PowerShell)

```powershell
copy springboot\models\med_word.txt springboot\src\main\resources\med_word.txt
```

### Linux/Mac

```bash
cp springboot/models/med_word.txt springboot/src/main/resources/med_word.txt
```

## ✅ 验证

训练时，Java代码会从 `classpath:med_word.txt` 读取词典文件。

如果文件不在 resources 目录，训练时会有警告：
```
⚠️  未找到词典文件 classpath:med_word.txt，将使用默认分词
```

## 📝 说明

- `models/med_word.txt` - 当前位置（可保留作为备份）
- `src/main/resources/med_word.txt` - Java方案需要的位置（必需）

---

**完成复制后，就可以使用 Java 全自动训练方案了！** 🚀

